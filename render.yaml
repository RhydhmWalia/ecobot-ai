services:
  - type: web
    name: ecobot-ai
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn api.index:app"
    envVars:
      - key: XAI_API_KEY
        sync: false
